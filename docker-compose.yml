services:
  vincela-web:          # ← This name must match Caddyfile
    build: .
    image: vincela-site:latest
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true